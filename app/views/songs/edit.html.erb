<div class="col-xs-1"></div>
  <div class="col-xs-10">

  <p>Edit Song</p>

  <%= form_for (@song) do |f| %>
  <%= render 'form', f: f %>
    <%= f.submit %>
    <%end%>

    <%= button_to "Delete", {:controller => :songs, :id => @song.id, :action => 'destroy' }, :method => :delete %>

</div>
